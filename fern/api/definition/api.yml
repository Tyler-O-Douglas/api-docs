name: api
auth: MyAuthScheme
auth-schemes:
  MyAuthScheme:
    header: Authorization
    type: basic
error-discrimination:
  strategy: status-code
environments:
  Production: https://api.meetstring.com
default-environment: Production
